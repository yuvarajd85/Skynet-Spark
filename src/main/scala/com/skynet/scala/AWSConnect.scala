package com.skynet.scala

import com.amazonaws._;

object AWSConnect {
  
  def main(args : Array[String]){
    
    val accessKey = "";
    val secretAccessKey = "";
    
//    val basisAWSCred = new BasicAWSCredentials(accessKey, secretAccessKey);
    
//    val s3 = AmazonS3ClientBuilder
  }
}